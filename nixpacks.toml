[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python -c 'from app import init_db; init_db()' && gunicorn app:app --bind 0.0.0.0:${PORT:-5000}"
